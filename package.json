{
  "name": "gitlab-commit-tree",
  "displayName": "GitLab Commit Tree",
  "version": "3.1.0",
  "description": "Affiche une arborescence des fichiers modifiés dans les commits, comparaisons et historiques de branches GitLab",
  "scripts": {
    "test": "jest",
    "dev": "vite build --mode development --watch",
    "build": "vite build",
    "package": "npm run build && mkdir -p build && cd dist && zip -r ../build/gitlab-commit-tree.zip *"
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "type": "module",
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",
    "babel-jest": "^30.2.0",
    "jest": "^30.0.0",
    "jest-environment-jsdom": "^30.0.0",
    "rollup-plugin-terser": "^7.0.2",
    "terser": "^5.46.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "diff": "^8.0.3",
    "highlight.js": "^11.11.1"
  }
}
